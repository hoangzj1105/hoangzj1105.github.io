# Portfolio — Hồ Văn Hoàng

Portfolio tĩnh (HTML/CSS/JS) để nộp CV freelance.

## Cấu trúc
- `index.html` — trang chính
- `styles.css` — style
- `app.js` — javascript nhẹ xử lý form
- `assets/` — avatar, CV pdf, favicon

## Chạy cục bộ
Mở `index.html` bằng trình duyệt, hoặc dùng static server:
